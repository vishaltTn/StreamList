/**
 * Local font assets for React Native CLI linking.
 * Add Manrope and Inter files under ./assets/fonts/ (see that folder’s README).
 * After adding or changing font files, run: npx react-native-asset
 * Then rebuild the iOS/Android app (fonts are copied into native projects).
 */
module.exports = {
  assets: ['./assets/fonts'],
};
